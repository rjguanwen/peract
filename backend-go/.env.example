# ============================================================
# 躬行后端配置样例：复制为 .env 后按需修改
#   copy .env.example .env   (Windows)
#   cp .env.example .env     (Linux/macOS)
# 留空的项走代码内默认值（见各项注释），未列出的键不会被读取。
#
# 身份与治理层(账号、口令、组织、菜单、角色授权)由 OneLink 接管, 所以这里**没有**
# SECRET_KEY、没有初始管理员、也没有登录限流那一组配置 —— 它们保护的接口都不存在了。
# ============================================================

# ============ 基础 ============
PROJECT_NAME=躬行
# 运行环境：development（默认）/ production
# production 下缺 OneLink 配置、或 CORS 配了 * 会直接拒绝启动
APP_ENV=development

# ============ OneLink 接入（生产必填，三项缺一不可） ============
# 平台根地址, 形如 https://sso.corp.com。用来换票据、续期令牌、查会话存活。
ONELINK_BASE_URL=
# 门户地址。会话失效时把人送回这里(前端也会用它)。
ONELINK_PORTAL_URL=
# 应用密钥。**只在服务端**: 不进前端产物、不进日志、不进版本库。
# 它在平台侧只以摘要形式存储, 因此只有登记那一刻能拿到明文 —— 丢了只能重置。
ONELINK_APP_SECRET=
# 应用编码。必须与 OneLink 里登记的一致, 它同时是权限点前缀(权限点必须形如
# <应用编码>:xxx, 用 platform: 会被平台拒)。
ONELINK_APP_CODE=task-system
# 多久回平台问一次"这条会话还算不算一次登录"（秒）。
# 它直接决定**单点登出的可感知滞后**: 平台登出后, 躬行最多滞后一个周期才跟着下线。
# 下限 5 秒, 默认 60。调小它比"在关键操作前额外轮询一次"正确 —— 后者会让应用代码里
# 出现两个关于"这个人还在不在线"的真值来源。
ONELINK_ALIVE_INTERVAL_SECONDS=60
# 权限清单端点挂在哪条路径上, 默认 /onelink/perm-manifest。
# 平台在管理台上点"拉取权限点"时会**主动请求**这个路径(见 internal/perms 与
# handler.RegisterRoutes), 所以要在 OneLink 的应用管理里把这个地址填进
# "权限清单拉取地址" —— 两边不一致的表现是平台侧一句"拉取失败", 看不出是路径写错
# 还是签名没过(签名用 ONELINK_APP_SECRET, 不需要额外配凭据)。
# 它挂在登录守卫之外(调用方是平台, 那里没有会话), 守卫是平台签名; 部署时还应当让
# 网络层再收一层: 只允许平台的出口地址访问它。
ONELINK_PERM_PULL_PATH=/onelink/perm-manifest

# 未读数(用户态)端点挂在哪条路径上, 默认 /onelink/unread。
# 平台画门户上躬行那张卡片的角标时会**主动请求**这个路径, 所以要在 OneLink 的应用管理里
# 把它填进"未读数拉取地址"。与上面那条的区别只在被问的对象: 那条问"躬行有哪些权限点",
# 这条问"**某个人**在躬行有多少未读提醒" —— 平台会带上会话里的 userId, 并把它签进签名串。
#
# 它同样挂在登录守卫之外, 守卫也是平台签名, 同样应当让网络层只允许平台出口访问。
# 留空则不挂这条路由, 那时卡片不显示角标(而**不是**显示 0)。填错的表现很安静: 卡片上
# 只有一个灰色问号, 两侧都不会报错 —— 所以它在启动日志里会打出来, 照着抄。
ONELINK_UNREAD_PATH=/onelink/unread

# ============ 服务与数据库 ============
# Go 后端端口
PORT=8001
# 架构上固定 SQLite（纯 Go 驱动，无需 CGO，数据库文件就在 backend-go 目录下）。
# 填 postgres:// / mysql:// 等其他引擎连接串会在启动时直接报错退出，
# 不会被当成裸路径静默建出一个同名 SQLite 文件。
DATABASE_URL=sqlite:///./task.db
# 对外前端地址
APP_BASE_URL=http://localhost:5177
# 上传文件根目录（头像等），会以静态目录 /uploads 暴露
UPLOAD_DIR=./uploads

# ============ 运行期保护 ============
# 允许跨域的来源，逗号分隔。
# **不要用 `*`**: 会话是一张凭据型 cookie, 浏览器不会在带凭据的跨域请求上接受通配来源,
# 于是跨域登录会静默失败(现象是"每个请求都 401"), 而生产环境会直接拒绝启动。
CORS_ALLOW_ORIGINS=http://localhost:5177
# 反向代理 IP 或 CIDR，逗号分隔。留空则完全不采信 X-Forwarded-For。
# 置于 Nginx 之后必须填写, 否则来源 IP 可被伪造头改写。
TRUSTED_PROXIES=
# 单个请求体上限（字节），默认 12MB，需容纳 5MB 头像表单
MAX_BODY_BYTES=12582912
# 优雅停机等待在途请求的上限（秒）
SHUTDOWN_TIMEOUT_SECONDS=10

# ============ 提醒通知（可选） ============
# 外部通知走异步队列投递，不阻塞请求；队列满则丢弃并记日志
NOTIFY_WORKERS=2
NOTIFY_QUEUE_SIZE=256
# IM Webhook（钉钉/企业微信/飞书），留空则不推送
NOTIFY_WEBHOOK_URL=
NOTIFY_WEBHOOK_TYPE=generic        # generic / dingtalk / wecom / feishu
WEBHOOK_TIMEOUT_SECONDS=5
# SMTP 邮件，HOST/USER/FROM 三者齐全才启用；密码亦可使用 SMTP_PASS 键名
SMTP_HOST=
SMTP_PORT=465
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=
SMTP_FROM_NAME=躬行
SMTP_USE_TLS=true
SMTP_DIAL_TIMEOUT_SECONDS=10
SMTP_COMMAND_TIMEOUT_SECONDS=20

# ============ 权限点登记（不在这里配，只作说明） ============
# 躬行的权限点必须登记在 OneLink 上, 归属是 ONELINK_APP_CODE 那个应用。
# 四种形态, 写的是同一批权限点:
#   1) 上报（部署时, 适合持续集成）: go run ./cmd/perm-sync
#      它把 internal/perms.Manifest 提交给 POST /open/v1/permissions/sync,
#      用上面的 ONELINK_APP_SECRET 验签 —— 不需要任何人的令牌, 所以在一个人都还没
#      登录过的新环境里也能跑。加 -dry-run 只解析不联网。
#   2) 拉取（两次部署之间）: 平台管理台 → 应用管理 → 该应用的「集成」列 → 拉取权限点。
#      平台按 ONELINK_PERM_PULL_PATH 那个地址来取, 取到的就是 1) 提交的同一份清单。
#      它不需要在这边跑任何东西, 但要求本进程在跑(端点在服务里)。
#   3) 管理台手工维护: 同一个「集成」列 → 菜单权限 / 角色授权
#      (走 /api/v1/admin/apps/:id/permissions 那一族路由, 只对平台超管开放)
#   4) SQL 种子: deploy/seed-perms.sql（没有流水线、也没有超管在场时的兜底）
#
# 清单只有**一个事实源**: internal/perms/manifest.go。1) 与 2) 读的都是它, 所以这两条
# 路径不可能给出两份不同的清单。deploy/perms.manifest.json 是它的部署产物镜像(给人读、
# 给 seed-perms.sql 生成用), 两者相等由 internal/perms 的用例保证 —— 改一处忘了另一处时
# `go test ./...` 会指名是第几条的哪个字段。
# 代码里用到的码见 internal/handler/handler.go 顶部那组常量。
