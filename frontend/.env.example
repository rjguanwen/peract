# 躬行前端的开发期/构建期变量。
#
# 用法: 复制成同目录下的 .env 即可生效
#
#     cp .env.example .env
#
# .env 已被根目录的 .gitignore 忽略(第 2 行), 不会进版本库; 而这个 .example 文件**必须**进
# 版本库(第 3 行的 !.env.example 就是为它留的) —— 它是"有哪些旋钮、各自默认是什么"的唯一
# 事实源, 少了它, 下一个人只能靠读源码去猜, 而猜错的表现通常是一句与配置无关的报错。
#
# ────────────────────────────────────────────────────────────────────────────────
# 一条硬约束: 只有 VITE_ 前缀的变量会被 Vite 注入到**浏览器代码**里(import.meta.env)。
#
# 反过来说: 写进这个文件的 VITE_ 变量, 最终会原样出现在前端产物里 ——
# **密钥、口令、内部地址一律不要放这里**。应用密钥(ONELINK_APP_SECRET)在后端那边,
# 它永远不该出现在这个文件里, 也不该出现在任何前端产物里。
# ────────────────────────────────────────────────────────────────────────────────

# 门户地址。会话在浏览器里失效时, 前端把人送回这里(见 src/utils/portal.js)。
#
# 留空则**只提示、不跳转** —— 这不是偷懒: 跳到一个空字符串会让浏览器留在当前页, 用户看到
# 的是"点了退出什么也没发生", 而那比一句明确的提示更难查。本地还没搭起 OneLink 时可以留空,
# 但业务路由本身仍然要求一张有效会话(见 README 的"怎么进去")。
#
# 两条一致性要求, 写错的表现都离配置很远:
#   1. 必须与后端那边的 ONELINK_PORTAL_URL 指向**同一个门户**;
#   2. 必须与门户实际监听的地址**逐字**一致 —— 门户的 redirect_uris 是前缀匹配, 而
#      http://127.0.0.1:5173 与 http://localhost:5173 是两个不同的 Origin。写错的典型
#      表现是"登录能成, 但回跳被拒", 而那句话里不会提到端口或主机名。
VITE_ONELINK_PORTAL_URL=http://127.0.0.1:5173

# 后端端口。只影响 dev server 的代理目标(vite.config.js 里的 /api、/sso、/logout 三条),
# **不影响生产构建** —— 生产环境里前端把 /api 打到哪, 由部署时的反向代理决定。
#
# 后端(backend-go)默认监听 8001, 所以只在改了后端端口时才需要动它。
VITE_BACKEND_PORT=8001
